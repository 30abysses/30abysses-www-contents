# C# vs. TypeScript: Basic Types, Dynamic, Any


# C# Dynamic

* Keyword: `dynamic`.
* C# compiler assumes that `dynamic` entities can support any operation.


# TypeScript Any

* Keyword: `any`
* TypeScript compiler assumes that `any` entities can support any
  operation.
